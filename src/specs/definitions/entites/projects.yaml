definitions:

  Project:
    type: object
    properties:
      name:
        type: string
      description:
        type: string
      fromDate:
        type: string
      toDate:
        type: string
      company:
        allof:
          - $ref: "./companies.yaml#/definitions/Company"
      users:
        type: array
        users:
            - $ref: "./users.yaml#/definitions/User"
      appointments:
        type: array
          appointments:
            - $ref: "./appointments.yaml#/definitions/Appointment"

